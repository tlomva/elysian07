name: Build jar to Release

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write 

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v6

    - name: Set up JDK 17
      uses: actions/setup-java@v5
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Build with Maven
      run: |
        mvn clean package \
          -Dcheckstyle.skip=true \
          -Dmaven.compiler.source=17 \
          -Dmaven.compiler.target=17
        
        # 重命名主JAR文件
        mv bootstrap/target/BungeeCord.jar bungeecord.jar
        ls -la bungeecord.jar

    - name: Create Release
      uses: ncipollo/release-action@v1
      with:
        artifacts: "bungeecord.jar"
        tag: ${{ github.ref_name }}
        name: "bungeecord ${{ github.ref_name }}"
        token: ${{ secrets.GITHUB_TOKEN }}
        allowUpdates: true
        replacesArtifacts: true
        body: "Automated release from workflow run #${{ github.run_number }}"
